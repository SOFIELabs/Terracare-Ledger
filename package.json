{
  "name": "terracare-ledger",
  "version": "0.1.0",
  "private": true,
  "description": "Terracare Ledger — tokenless, permissioned PoA chain for Heartware and Sofie-Systems OS",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "lint": "echo 'Add solhint/ eslint if needed'",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "deploy:custom": "hardhat run scripts/deploy.js --network terracare"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.27.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
